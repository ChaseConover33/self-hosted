#!/usr/bin/env bash
set -euo pipefail

host="${1:-}"

if [[ -z "${host}" ]]; then
  echo "usage: $0 <host>" >&2
  exit 1
fi

ssh "${host}" "systemctl status self-hosted-backup.timer --no-pager && ls -lh /srv/self-hosted/backups | tail"
