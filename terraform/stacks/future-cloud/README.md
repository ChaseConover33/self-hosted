# Future Cloud Provisioning

Terraform is intentionally deferred in v1.

This directory exists so future AWS, GCP, or other single-VM provisioning can be added without restructuring the rest of the repo.
