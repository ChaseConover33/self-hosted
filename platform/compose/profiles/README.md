# Optional Profiles

This directory is reserved for future optional profiles such as:

- local AI experiments
- notes/docs tools
- local DNS helpers
- provider-specific additions

The initial implementation keeps these out of the default stack.
