# Chronicle production secrets
# Get these from https://dashboard.clerk.com (production instance!) and Anthropic console.

# Clerk — production keys (must be production instance, not test)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=REPLACE_ME
CLERK_SECRET_KEY=REPLACE_ME

# Clerk URL routing — sign-up points back to sign-in because Phase 1 disables signups
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# AI provider keys (only Anthropic is required; others can be added later)
ANTHROPIC_API_KEY=REPLACE_ME
